<!DOCTYPE html>
html(lang="zh-TW")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        meta(property="og:image" content="./img/filter-img.png")
        link(href="https://unpkg.com/ionicons@4.1.2/dist/css/ionicons.min.css" rel="stylesheet")
        link(rel="stylesheet", href="./css/style.css")
        title Filter 高雄旅遊景點 - 前端修煉賽事第二週
    body
        #app
            //- header
            header.header
                .container
                    .logo
                        a(href="./") HaveFun
                    .search
                        i.i.ion.ion-md-search
                        input(type="text" placeholder="請輸入景點名稱" v-model="filterText" @keyup.enter="filterKeyWord")

            .container
                //- 搜尋工具列
                aside.side
                    .side-location
                        h2.subtitle 地區
                        .select-style
                            select(name="" v-model="zoneText" @change="filterMix")
                                option(value="") 選擇地區
                                option(:value="item" v-for="item in zone") {{item}}

                    .side-categories
                        h2.subtitle 進階篩選
                        .input-group
                            .checkbox-style(:class="{'active' : free}")
                                input(v-model="free" type="checkbox" value="entertainment" id="free" @change="filterMix")
                                label(for="free") 免費參觀
                        .input-group
                            .checkbox-style(:class="{'active' : open}")
                                input(v-model="open" type="checkbox" value="food" id="open" @change="filterMix")
                                label(for="open") 全天候開放

                //- 內容區塊
                article.content
                    .content-top
                        p.p Showing <span>{{filter.length}}</span> resulits By ...
                        .btn-group
                            .btn.btn-close(v-if="zoneText" @click="zoneText = ''") {{zoneText}}
                            .btn.btn-close(v-if="free" @click="free = false") 免費參觀
                            .btn.btn-close(v-if="open" @click="open = false") 全天候開放
                    ul.contents
                        li.content-item(v-for="item in filter")
                            a(href="./content.html")
                                .content-img.imgbox
                                    .imgbox-inner
                                        img.img(:src="item.Picture1", alt="img")
                                .content-info
                                    h2.subtitle {{item.Name}}
                                    p.content-des {{item.Description}}
                                    .content-ticket
                                        h3 門票說明
                                        .content-tag(v-if="item.Ticketinfo") {{item.Ticketinfo}}
                                        .content-tag(v-else) 無
                                    .content-addr
                                        i.ion.ion-md-locate
                                        p {{item.Add}}
                                    .content-open
                                        i.ion.ion-md-calendar
                                        p {{item.Opentime}}
                                    .content-webside
                                        a(v-if="item.Website" :href="item.Website" target="_blank")
                                            i.ion.ion-ios-link

                    .content-paging
                        .page-item.page-prev
                            i.ion.ion-md-arrow-dropleft
                        .page-item.active(data-item="1") 1
                        .page-item(data-item="2") 2
                        .page-item(data-item="3") 3
                        .page-item(data-item="4") 4
                        .page-item(data-item="5") 5
                        .page-item.page-next
                            i.ion.ion-md-arrow-dropright

            //- footer
            footer.footer
                p.footer-text Filter - The F2E 前端修練精神時光屋 [第二週賽事] | 高雄旅遊網API應用 | Front End Development by HSI-MAO CHEN
                a.footer-link(href="https://github.com/hsimao/" target="_blank")
                    i.icon.ion-logo-github
                a.footer-link(href="https://codepen.io/hsimao" target="_blank")
                    i.icon.ion-logo-codepen
        script(src="https://unpkg.com/axios/dist/axios.min.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/vue/2.5.16/vue.js")
        script(src="./js/js.js")